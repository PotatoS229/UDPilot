# Устанавливаем Fastify
npm install fastify

# Устанавливаем библиотеку для IPC (выберите одну из опций)
npm install node-ipc        # Универсальная библиотека для IPC
# или
npm install socket.io       # Если нужен WebSocket
# или используем встроенный модуль net (не требует установки)